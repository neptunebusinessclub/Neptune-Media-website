{
  "name": "neptune-media-cloudflare",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:temporary": "wrangler deploy --temporary",
    "check": "node --check src/index.js && node --check src/store.js && node --check src/security.js && node --check public/app.js && node --check public/studio/studio.js",
    "test:smoke": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.111.0"
  }
}
